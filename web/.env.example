# Runtime config for the Hono web/BFF service. Read by web/server.mjs.
PG_PORT=8080
PG_STATIC_DIR=build
# Rust server URL (required at boot)
PG_SERVER_URL=
# Shared secret sent as X-API-Key. Must match the server's PG_API_KEYS.
PG_API_KEY=
