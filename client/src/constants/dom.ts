export enum ClassName {
  ERROR = "error",
  OPEN = "open",
  HIDDEN = "hidden",
  TOUCHED = "touched",
  SELECTED = "selected",
  CTX_SELECTED = "ctx-selected",
  FILE = "file",
  FOLDER = "folder",
  SIDE_RIGHT = "side-right",
}

export enum Id {
  ROOT_DIR = "root-dir",
}
