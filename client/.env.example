# Override the "Solana Foundation" build server endpoint. Set this in
# preview/staging builds (e.g. Vercel) to the temporary App Engine URL until
# the permanent foundation domain is bound; unset uses the built-in default.
REACT_APP_FOUNDATION_SERVER_URL=https://playground-server-dot-analytics-324114.de.r.appspot.com
