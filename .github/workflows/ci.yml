name: CI

# Pull-request validation gate. Deployment is handled separately by cicd.yml
# (triggered by tag pushes or manual dispatch), so this runs only on PRs.
on:
  pull_request:
    branches:
      - master

  workflow_dispatch:

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
