name: CI

# Pull-request gate. Pushes to master are validated + deployed by cicd.yml,
# so this workflow intentionally does not trigger on push.
on:
  pull_request:
    branches:
      - master

  workflow_dispatch:

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
